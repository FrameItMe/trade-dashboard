# Example environment variables (safe to commit)
# Copy to .env.local and fill secrets locally.
NEXT_PUBLIC_API_URL=
# SECRET_API_KEY is server-only and should NOT be committed. Leave blank here.
SECRET_API_KEY=

# Supabase public (anon) keys for client usage
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_KEY=

# Add more variables as needed.